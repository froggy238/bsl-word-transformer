# CPU-only torch wheels (avoids pulling CUDA builds on HF Spaces)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
mediapipe==0.10.9
opencv-contrib-python==4.9.0.80
numpy==1.26.4
gradio==4.44.1
pyyaml==6.0.1
# Imported transitively by the shared src/ package (src.dataset, src.extract)
pandas==2.2.2
tqdm==4.66.4
